# Kubernetes 네임스페이스 정의
# Agent 배포를 위한 네임스페이스 생성

apiVersion: v1
kind: Namespace
metadata:
  name: agentic-ai-ops
  labels:
    name: agentic-ai-ops
    environment: dev  # dev, staging, production으로 변경 가능
